import React, { useState } from 'react';

const Date = () => {
    const [currentTime, setCurrentTime] = useState(new Date())
    
    return (
        <div>
            
        </div>
    );
}

export default Date;
